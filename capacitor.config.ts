import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.crankitup.www',
  appName: 'Crank-it-up',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
